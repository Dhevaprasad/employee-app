apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: employee-ingress

spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix

        backend:
          service:
            name: employee-service

            port:
              number: 80